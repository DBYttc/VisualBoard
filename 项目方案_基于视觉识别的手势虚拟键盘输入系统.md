# 基于视觉识别的手势虚拟键盘输入系统 —— 项目方案

> 项目周期：9–12 周（2026 年 9 月启动）
> 项目性质：课程/竞赛项目（以可演示作品为核心）
> 方案版本：v1.0

---

## 一、项目概述

本项目制作一个**基于视觉识别的键盘输入系统**：通过摄像头实时识别用户手部动作，将手指在屏幕虚拟键盘上的悬停/捏合操作转换为真实的系统级键盘输入，无需接触任何物理键盘即可完成打字。

**核心体验**：摄像头对准手部 → 屏幕显示虚拟 QWERTY 键盘 → 移动指尖选中按键（高亮反馈）→ 捏合/悬停触发按键 → 文字输入到任意应用程序（记事本、Word、浏览器等）。

---

## 二、所需材料清单

### 2.1 硬件（总预算约 200–500 元）

| 材料 | 规格建议 | 用途/说明 |
|---|---|---|
| 电脑 | 现有笔记本即可（i5/R5 + 8GB 内存）；有独显更好但**非必需**——MediaPipe 在 CPU 上就能跑 30fps | 运行程序 |
| 摄像头 | 720p/1080p、30fps USB 摄像头（罗技 C270 约 ¥100 / C920 约 ¥400）；**内置摄像头可先起步** | 采集手部图像 |
| 摄像头支架/三脚架 | 约 ¥30，可调角度 | 固定摄像头俯拍手部区域 |
| 补光灯/台灯 | 约 ¥50 | **光照对手部检测影响最大**，演示环境必备 |
| （可选）纯色桌垫/背景布 | 约 ¥20 | 减少背景干扰，提高检测稳定性 |
| （可选）USB 延长线 | 约 ¥15 | 灵活布置摄像头位置 |

### 2.2 软件环境（全部免费）

```
Python 3.10/3.11（推荐，与 mediapipe 兼容性最稳）
pip install opencv-python mediapipe numpy pynput
# 可选：pip install pyautogui cvzone
```

- IDE：VS Code / PyCharm
- Git + GitHub（版本管理 + 竞赛作品展示）

---

## 三、系统架构

五层处理流水线：

```
① 摄像头采集（OpenCV VideoCapture）
      ↓
② 手部关键点检测（MediaPipe Hands，输出 21 个 landmarks）
      ↓
③ 指尖坐标 → 虚拟键盘按键映射
      ↓
④ 手势触发判定（捏合点击 / 悬停计时）
      ↓
⑤ pynput 系统级按键输出 + 屏幕文本显示
```

---

## 四、9–12 周制作方案

> 按 12 周排期；若周期压缩至 9 周，则合并第 8+9 周、第 10+11 周任务。

| 周次 | 阶段 | 任务 | 验收标准 |
|---|---|---|---|
| **W1** | 调研与准备 | 阅读参考项目，确定交互方式（捏合 vs 悬停计时）；搭环境，跑通 MediaPipe 官方 hands demo | 摄像头画面实时显示手部骨架 |
| **W2** | 检测管线 | VideoCapture + MediaPipe Hands 实时输出 21 个关键点，测试不同光照/距离下的稳定性 | 30fps 稳定跟踪单手 |
| **W3** | 指尖映射 | 提取食指/拇指指尖坐标，先做一个"指尖移动鼠标指针"小 demo 验证映射逻辑 | 指尖可控移动光标 |
| **W4** | 键盘 UI | Button 类 + QWERTY 布局绘制（含数字行、space/backspace/enter/caps），悬停高亮反馈 | 虚拟键盘渲染 + 高亮 |
| **W5** | 手势触发 | 捏合点击判定（食指-拇指距离 < 阈值）+ 冷却防误触；实现悬停计时点击，两种方式对比选主交互 | 单指悬停/捏合能稳定选键 |
| **W6** | 系统输入 | pynput `Controller` 模拟按键，实现与记事本/Word 等**任意应用**无缝输入；功能键全部打通 | 在系统记事本中完成完整句子输入 |
| **W7** | 稳定性优化（中期节点） | 指尖抖动 → 指数移动平均/一维卡尔曼滤波平滑；点击冷却（0.3–0.5s）、阈值自适应；不同光照测试 | 误触率明显下降 |
| **W8** | 中文输入（加分项） | 走 pynput 输出到**系统拼音输入法**（天然支持中文，零额外开发）；进阶可做自绘中文九宫格 | 能输入中文句子 |
| **W9** | 性能与体验 | 帧率优化（检测节流、多线程）、UI 美化、新手引导/操作提示、演示模式 | 流畅度 + 观感达标 |
| **W10** | 扩展功能（竞赛加分） | 模式切换：鼠标控制模式（指尖移动 + 捏合点击/拖动）；自定义手势切模式（如"剪刀手"） | 键鼠双模式可切换 |
| **W11** | 测试与整理 | 可用性测试并统计**输入速度（WPM）和准确率**（竞赛答辩硬指标）；录制演示视频、写 README | 有量化数据 + 演示视频 |
| **W12** | 展示准备 | 答辩/演示 PPT、现场预案（演示前标定距离、带补光灯、备好一键复位） | 现场演示一次通过演练 |

---

## 五、关键技术点与常见坑

1. **mediapipe 版本坑**：0.10.x 后 API 有变化，建议固定 `mediapipe==0.10.x` 使用 `mp.solutions.hands`，或直接用 cvzone 的 `HandTrackingModule` 封装（教程最多、最省事）
2. **画面镜像**：摄像头画面必须水平翻转（`cv2.flip`），否则左右手操作是反的
3. **防误触三件套**：捏合距离阈值 + 按下冷却时间 + 指尖轨迹平滑（EMA/卡尔曼），这是从"能演示"到"好用"的关键
4. **演示环境**：现场光照往往差，务必自带补光灯、提前标定摄像头距离；程序里做"一键复位/重新检测"入口
5. **pynput 在 Windows 上可能被个别杀毒软件误报**，答辩前在目标电脑上先验证一遍

---

## 六、参考项目（可直接克隆学习）

| 项目 | 特点 |
|---|---|
| [amrKhaled2024/Virtual-Floated-Keyboard](https://github.com/amrKhaled2024/Virtual-Floated-Keyboard) | 捏合点击（食指-拇指距离）+ PyAutoGUI，含 Caps/Backspace |
| [RayVader987/Gesture-Type](https://github.com/RayVader987/Gesture-Type) | 悬停计时点击，功能最全（符号、命令启动应用） |
| [Nikeshpatel04/Virtual-Keyboard](https://github.com/Nikeshpatel04/Virtual-Keyboard) | 与本项目需求最贴合：OpenCV + MediaPipe + **pynput** 无接触打字 |
| [RubaNadeem/AI-Powered-Virtual-Keyboard](https://github.com/RubaNadeem/AI-Powered-Virtual-Keyboard) | 悬停选键 + 完整功能键 |
| [CSDN 中文教程：使用 mediapipe+opencv 做一个简单的虚拟键盘](https://blog.csdn.net/m0_58772523/article/details/121455180) | cvzone 方案，含 Button 类和键盘绘制逻辑，适合起步 |
| [MediaPipe 官方 Hands 文档](https://ai.google.dev/edge/mediapipe/solutions/vision/hand_landmarker) | 官方 API 参考 |

**推荐学习路线**：先跑通 CSDN 的 cvzone 版教程（1 天即可有雏形）→ 参考 Gesture-Type 补齐功能键和防误触 → 用 pynput 替换输出层实现系统级输入。三者优点拼起来就是完整作品。

---

## 七、分工建议（供组长参考）

- **算法/核心逻辑**（1–2 人）：手部检测、指尖映射、触发判定、滤波平滑（W2–W5、W7）
- **UI/交互**（1 人）：虚拟键盘绘制、高亮反馈、模式切换界面（W4、W9–W10）
- **系统集成/测试**（1 人）：pynput 输出、应用兼容性测试、WPM/准确率统计（W6、W11）
- **文档/演示**（1 人，可兼任）：README、演示视频、答辩 PPT（贯穿全程，重点 W11–W12）
